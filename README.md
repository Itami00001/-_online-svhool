- **courses** - курсы
- **lessons** - уроки
- **enrollments** - записи студентов на курсы

## 🛠️ Установка и запуск

### Предварительные требования

- Docker Desktop
- Docker Compose

### Настройка окружения

> [!IMPORTANT]
> **Безопасность**: Проект использует переменные окружения для хранения конфиденциальных данных.

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте файл `.env` и измените пароль базы данных:
```env
DB_PASSWORD=your_secure_password_here  # Замените на свой пароль
```

### Запуск проекта

1. Клонируйте репозиторий или перейдите в директорию проекта:
```bash
cd bd_test
```

2. Запустите Docker контейнеры:
```bash
docker-compose up -d
```

3. Откройте браузер и перейдите по адресу:
```
http://localhost:3000
```

### Остановка проекта

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f web
```

## 📊 API Endpoints

- `GET /api/students` - получить всех студентов
- `GET /api/teachers` - получить всех преподавателей
- `GET /api/courses` - получить все курсы
- `GET /api/lessons/:courseId` - получить уроки курса
- `GET /api/enrollments` - получить все записи на курсы
- `GET /api/stats` - получить статистику
- `POST /api/students` - добавить студента
- `POST /api/enrollments` - записать студента на курс

## 🔐 Доступ к базе данных

Параметры подключения настраиваются в файле `.env`:

```env
DB_HOST=localhost      # или 'db' внутри Docker
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password  # Ваш пароль из .env
DB_NAME=online_school
```

## 👨‍💻 Автор

Белялов Таир, И-1-24